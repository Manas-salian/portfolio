---
export interface Props {
  tag: string;
  tagName: string;
  size?: "sm" | "lg";
}

const { tag, tagName } = Astro.props;
---

<li style="display:inline-block;">
  <a href={`/tags/${tag}/`} class="tag">#{tagName}</a>
</li>
